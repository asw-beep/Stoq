"""Forecasting engine (Phase 2).

A modular-monolith package mirroring ``market_data/``: pure feature functions,
swappable model implementations behind a ``Forecaster`` Protocol, a repository
for persistence, and a service that orchestrates the pipeline.
"""
